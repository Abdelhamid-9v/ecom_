function deliverPizza() {
    console.log("Pizza is here! Time to eat.");
}

function orderPizza(topping, ()=> ca    
) {
    console.log(`Ordering a ${topping} pizza...`);
    // Once the "work" is done:
    callback(); 
}

orderPizza("Pepperoni", deliverPizza);